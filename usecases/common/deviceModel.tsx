export const mapDeviceTypeActionOptions: Record<
  string,
  { action: string; name: string }[]
> = {
  bot_switch: [
    { action: 'on', name: 'Turn On' },
    { action: 'off', name: 'Turn Off' },
  ],
};
